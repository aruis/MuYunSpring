package net.ximatai.muyun.spring.platform.metadata;

/** Query semantics of a field UI control, independent of a concrete UI adapter. */
public enum FieldUiControlQueryMode {
    DEFAULT,
    BETWEEN
}
